### ✔️ Nunca permita:

* lançar em conta sintética

```sql
ALTER TABLE plano_contas
ADD CONSTRAINT chk_analitica
CHECK (tipo IN ('A','S'));
```

👉 Valide no backend:

```sql
-- conta.tipo deve ser 'A'
```

---

### ✔️ Índices importantes

```sql
CREATE INDEX idx_lancamento_data ON lancamento(data);
CREATE INDEX idx_lancamento_item_conta ON lancamento_item(conta_id);
